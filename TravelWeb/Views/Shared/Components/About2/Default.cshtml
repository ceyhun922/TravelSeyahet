@model List<About2>
<section class="w3l-grids1">
  <div class="hny-three-grids py-5">
    <div class="container py-lg-5">
      <div class="row">
        @foreach (var item in Model)
        {
          <div class="col-md-4 col-sm-6 mt-0 grids5-info">
            <a href="#url"><img src="@item.About2Image" class="img-fluid" alt="@item.About2Title1"></a>
            <h5>@item.About2Title1</h5>
            <h4><a href="#url">@item.About2Title2</a></h4>
            <p>@item.About2Description</p>
          </div>
        }
      </div>
    </div>
  </div>
</section>