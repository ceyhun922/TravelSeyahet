<li class="nav-item nav-profile">
              <a href="#" class="nav-link">
                <div class="nav-profile-image">
                  <img src="@Model.WriterImage"" alt="@ViewBag.Name" />
                  <span class="login-status online"></span>
                  <!--change to offline or busy as needed-->
                </div>
                <div class="nav-profile-text d-flex flex-column">
                  <span class="font-weight-bold mb-2">@User.Identity?.Name</span>
                  <span class="text-secondary text-small">Müştəri</span>
                </div>
                <i class="mdi mdi-bookmark-check @(Model.WriterStatus == true ? "text-success" : "text-danger") nav-profile-badge"></i>
              </a>
            </li>